---
title: Release Channels
description: Stable, preview, and canary channels coordinate CLI and VS Code rollouts.
section: Release Channels
order: 10
surfaces:
  - cli
  - vscode
  - admin
related:
  - /docs/changelog/2026-04-foundation
---

# Release Channels

Diffmint should publish coordinated release metadata for both the CLI and VS Code extension.

## Channels

- `stable` for the default production audience
- `preview` for early adopters
- `canary` for internal testing and fast feedback

Each release manifest should include version, checksum, download target, and changelog URL.

For production rollouts, Diffmint can also sign release manifests before returning them from the
client API. When `DIFFMINT_RELEASE_SIGNING_PRIVATE_KEY` is configured, manifests include a stable
`signature` envelope with:

- `algorithm` set to `ed25519`
- `keyId` for key rotation and rollout tracking
- `signedAt` anchored to the manifest release timestamp
- `value` as the base64 signature over the canonical manifest payload
