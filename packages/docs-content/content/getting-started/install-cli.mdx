---
title: Install the CLI
description: Set up the Diffmint terminal workflow and connect it to the control plane.
section: Getting Started
order: 20
surfaces:
  - cli
  - admin
related:
  - /docs/cli/reference
  - /docs/concepts/local-first-review
---

# Install the CLI

The CLI is the canonical entry point for reviews, explain flows, test generation, history, and diagnostics.

## Install

```bash
pnpm install
```

## Sign in and select a workspace

```bash
dm auth login
dm config set-provider qwen
dm doctor
```

The browser step now lands on a dedicated device approval page. Sign in, switch to the correct
workspace if needed, approve the device session, then return to the terminal.

If `qwen` is available on your machine and your provider credentials are configured, review runs
will use Qwen headless mode automatically. Otherwise Diffmint stays in scaffold mode so onboarding
and local testing still work.

## Typical review commands

```bash
dm review --staged
dm review --base origin/main
dm review --files src/app/page.tsx src/lib/review.ts
dm explain src/app/page.tsx
dm tests src/lib/review.ts
```

See the full [CLI reference](/docs/cli/reference) for flags and JSON output.

For containerized local development with hot reload, see [Docker development](/docs/getting-started/docker-development).
